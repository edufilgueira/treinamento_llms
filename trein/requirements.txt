# Treino e utilitários (train_lora, merge_lora, inferir, build_snapshot, verificar_ambiente)
# Sem API nem PostgreSQL. Instale com: pip install -r trein/requirements.txt
# (a partir da raiz do repositório; em CI podes apontar o caminho absoluto deste ficheiro)
# O ficheiro antigo da raiz juntava isto *e* o servidor: este é mais *leve* (sem FastAPI/Postgres);
# a parte pesada (torch+CUDA) é a mesma. treina.sh usa o venv .venv-trein por defeito.
# torch 2.11+ puxa metapacote cuda 13.0.2 (muitas wheels + wheel torch ~0.5GB); em /workspace
# estilo MFS/RunPod a instalação fica muito lenta. Por defeito limitamos a <2.11 (ex. 2.10.x).
# Para 2.11+: TREIN_TORCH_ALLOW_211=1 ./trein/treina.sh  ou  torch>=2.1.0  (sem tecto) neste ficheiro.
#
# Git (CLI) — não é pacote pip; instale no sistema.
#
# Não precisa de gptqmodel / optimum para LoRA em FP32 habitual. Se precisar de checkpoint AWQ/GPTQ no Hub:
#   pip install -r trein/requirements-quantized.txt
# apt update && apt install git -y
# apt install zip -y

torch>=2.1.0,<2.11.0
transformers>=4.40.0
datasets>=2.18.0
accelerate>=0.28.0
peft>=0.11.0
trl>=0.9.0
sentencepiece>=0.2.0
tiktoken>=0.7.0
protobuf>=4.25.0
